#version 330 core
layout(location = 0) in vec3 Pos; // position has attribute position 0
layout(location = 1) in vec2 TexCoord;
layout(location = 2) in vec3 Col; // specify a color output to the fragment shader
out vec2 sTexCoord;
out vec3 sCol;
uniform mat4 transform;
uniform mat4 model;
uniform mat4 view;
uniform mat4 projection;
void main()
{
   gl_Position = transform *	`1	`l vec4(Pos, 1.00f); // we give a vec3 to vec4’s constructor
   sTexCoord = TexCoord;  // output variable to dark-red
   sCol = Col;
};